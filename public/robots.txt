# Route Ledger is a client-rendered static application. Public routes may be
# crawled; authenticated content remains protected by Clerk and Convex rather
# than robots directives.
User-agent: *
Allow: /
